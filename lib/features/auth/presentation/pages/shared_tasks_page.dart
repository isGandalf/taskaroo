import 'package:flutter/material.dart';

class SharedTasksPage extends StatelessWidget {
  const SharedTasksPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
